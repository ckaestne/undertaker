Choice	CHOICE_34	required	tristate
Depends	CHOICE_34	"USB_SUPPORT && USB_GADGET && USB_GADGET_SELECTED"
Item USB_ZERO boolean
ChoiceItem	USB_ZERO	CHOICE_34
Depends	USB_ZERO	"CHOICE_34"
Item USB_ZERO_HNPTEST tristate
ChoiceItem	USB_ZERO_HNPTEST	CHOICE_34
Depends	USB_ZERO_HNPTEST	"CHOICE_34 && USB_ZERO && USB_OTG"
/*
 * check-name: tristate choice with dependencies
 * check-output-start
I: Items-Count: 5
I: Format: <variable> [presence condition]
CONFIG_CHOICE_34 "(CONFIG_USB_SUPPORT && CONFIG_USB_GADGET && CONFIG_USB_GADGET_SELECTED) && !CONFIG_CHOICE_34_MODULE && ((CONFIG_USB_ZERO && !CONFIG_USB_ZERO_HNPTEST) || (!CONFIG_USB_ZERO && CONFIG_USB_ZERO_HNPTEST))"
CONFIG_CHOICE_34_MODULE "(CONFIG_USB_SUPPORT && CONFIG_USB_GADGET && CONFIG_USB_GADGET_SELECTED) && !CONFIG_CHOICE_34 && CONFIG_MODULES && !CONFIG_USB_ZERO && !CONFIG_USB_ZERO_HNPTEST"
CONFIG_USB_ZERO "CONFIG_CHOICE_34"
CONFIG_USB_ZERO_HNPTEST "(CONFIG_USB_ZERO && CONFIG_USB_OTG) && !CONFIG_USB_ZERO_HNPTEST_MODULE && CONFIG_CHOICE_34"
CONFIG_USB_ZERO_HNPTEST_MODULE "(CONFIG_USB_ZERO && CONFIG_USB_OTG) && !CONFIG_USB_ZERO_HNPTEST && CONFIG_MODULES && CONFIG_CHOICE_34_MODULE"
 * check-output-end
 */
